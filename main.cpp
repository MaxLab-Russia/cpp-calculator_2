// В этом файле только функция main и #include-директивы.
#include <iostream>
#include <string>
#include "calculator.h"

using Number = double;

int main() {
    // В main остался только вызов главной функции цикла
    RunCalculatorCycle();
    return 0;
}
